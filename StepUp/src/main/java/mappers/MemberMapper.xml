<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member.MemberDAO">
  <select id="getMemberList" resultType="member.MemberVO">
	 select 
			id, 
			name, 
			email, 
			create_date 
	  from 
		  member
  </select>
  <select id="getMember" parameterType="string">
	  select 
			  id, 
			  name, 
			  password, 
			  email 
	  from 
			  member 
	  where 
			  id = #{id}
  </select>
  
  <insert id="insertMember" parameterType="member.MemberVO">
	  insert into member 
	  		(id, name, password, email) 
	  values 
	  		(#{id},#{name}, #{password},#{email})
  </insert>
  
  <update id="updateMember" parameterType="member.MemberVO">
  	update 
  			member 
  	set 
  			name = #{name}, 
  			password = #{password}, 
  			email = #{email}
  	where 
  			id = #{id}
  </update>
  
  <delete id="deleteMember" parameterType="string">
  	delete 
  	from 
  			member 
  	where 
  			id = #{id}
  </delete>
</mapper>